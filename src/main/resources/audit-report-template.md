# Certificate Estate Assessment — {{CLIENT}}

> Generated by CertificateManager {{VERSION}} on {{FINISHED_AT}}. Sections marked
> **TODO (hand-written)** are for the auditor to write; everything else is filled from the run's
> evidence (`{{JSON_FILE}}`). Delete this note before sending.

| | |
|---|---|
| Requested by | {{REQUESTER}} |
| Basis | {{BASIS}} |
| Run | {{STARTED_AT}} to {{FINISHED_AT}} |
| Targets | {{TARGET_COUNT}} host:port rows from `{{CSV}}` |
| DNS resolver | {{RESOLVER}} |
| Trust anchors | {{TRUST_ANCHORS}} |

## 1. Executive summary

**TODO (hand-written):** three to five sentences: what was audited, the one or two findings that
matter most, and what to do first.

### Findings by severity

{{SEVERITY_COUNTS}}

### What this audit could not check

A check that failed or did not run is listed here. **Zero findings for a subject only means "clean"
when its checks ran.**

{{COVERAGE_GAPS}}

## 2. Certificates issued that no audited endpoint serves (CT)

{{UNOBSERVED}}

## 3. Issuance control (CAA)

{{CAA}}

## 4. Served state and chains

{{SERVED}}

## 5. Automation readiness and the March 2027 changes

**TODO (hand-written):** not measured by this audit kit (AUTO epics are not in Phase 0). From
2027-03-15 public TLS certificates last at most 100 days, and CAs must honour CAA `accounturi` and
`validationmethods`. Note which of the certificates in the inventory below are renewed by hand.

## 6. Ownership gaps

{{OWNERSHIP_GAPS}}

## 7. Remediation plan

**TODO (hand-written):** order the findings above into a short plan with owners and dates.

## Appendix A — Inventory

{{INVENTORY}}

## Appendix B — Input rows skipped

{{ROW_ERRORS}}

## Appendix C — Evidence

Every finding names its evidence. `observation host:port@address` refers to the `observations`
array in `{{JSON_FILE}}`; `cert <sha256>` to its `certificates` table; `https://crt.sh/?id=…` is the
public CT log entry; `caa …` names the DNS answer and the resolver that gave it. The full finding
list, with remediation text, is in `{{FINDINGS_CSV}}`.
